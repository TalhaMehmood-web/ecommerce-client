export interface ProductListTypes {
  id: string;
  productName: string;
  basePrice: number;
  discountedPrice: number;
  stockQuantity: number;
  productImage: string | null;
  createdAt: string;
}
